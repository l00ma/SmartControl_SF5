<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<div class="container-fluid">
		{% if is_granted('ROLE_USER') %}
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" href="{{ path ('welcome') }}">Home</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{ path ('user.edit') }}">Edit user</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{ path ('app_logout') }}">Sign out</a>
				</li>
			</ul>
		{% endif %}
	</div>
</nav>
